export interface ModeratorDoctor {
    id: string;
    firstName: string;
    secondName: string;
    lastName: string;
    uin: string;
}

export interface ModeratorCitizen {
    id: string;
    firstName: string;
    secondName: string;
    lastName: string;
    ucn: string;
}